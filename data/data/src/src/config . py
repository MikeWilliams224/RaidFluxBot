import os
from dotenv import load_dotenv
from pathlib import Path

load_dotenv()

TELEGRAM_TOKEN = os.getenv("TELEGRAM_TOKEN")
ADMIN_IDS = [int(x) for x in os.getenv("ADMIN_IDS", "").split(",") if x.strip()] or [6917296090]
DEFAULT_PROJECT = os.getenv("DEFAULT_PROJECT", "RaidFlux")
ENABLE_AUTOWELCOME = os.getenv("ENABLE_AUTOWELCOME", "true").lower() in ("1","true","yes")

BASE_DIR = Path(__file__).resolve().parent.parent
DATA_DIR = BASE_DIR / "data"
PROJECTS_FILE = DATA_DIR / "projects.json"
PROOFS_FILE = DATA_DIR / "proofs.json"
TEMPLATES_FILE = DATA_DIR / "templates.json"

# Rate limit / anti-spam
PROOF_COOLDOWN_SECONDS = 60  # per user minimal seconds between proofs (adjust)
